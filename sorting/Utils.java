package sorting;

public class Utils {
    /** calculates occurrence percentage of an element */
    static public int calculatePct(int occurrences, int listSize) {
        return (occurrences * 100) / listSize;
    }
}
